MedConnect AI microservice

This service uses OpenAI for LLM and Whisper for transcription. It also includes placeholders for ElevenLabs TTS.

To enable:
- Set OPENAI_API_KEY in env
- If using Whisper via OpenAI, ensure model availability and compliance
- ELEVENLABS_API_KEY can be used to synthesize audio if required
